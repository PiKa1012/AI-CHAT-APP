# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Pre-Commit Checklist

## 每次 commit 前询问用户是否要更新版本号

| 文件 | 字段 | 说明 |
|------|------|------|
| `app.json` | `expo.version` | 展示给用户的版本号，如 `"1.0.0"` |
| `app.json` | `expo.android.versionCode` | Android 内部版本号，每次+1 |
